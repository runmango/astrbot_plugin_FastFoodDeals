{
  "target_groups": {
    "description": "推送目标 QQ 群号列表",
    "type": "list",
    "items": {"type": "string"},
    "default": []
  },
  "target_brands": {
    "description": "监控的快餐品牌（如肯德基、麦当劳、德克士）",
    "type": "list",
    "items": {"type": "string"},
    "default": ["肯德基", "麦当劳", "德克士"]
  },
  "schedule_time": {
    "description": "每日定时发送时间，24 小时制，如 08:00",
    "type": "string",
    "default": "08:00"
  },
  "data_source": {
    "description": "数据源：mock 示例 / rss 订阅 / api 接口",
    "type": "string",
    "default": "mock",
    "enum": ["mock", "rss", "api"]
  },
  "rss_urls": {
    "description": "RSS 地址列表（data_source 选 rss 时必填）",
    "type": "list",
    "items": {"type": "string"},
    "default": []
  },
  "api_url": {
    "description": "数据 API 地址（data_source 选 api 时必填，返回 JSON 数组）",
    "type": "string",
    "default": ""
  },
  "api_method": {
    "description": "请求 API 的 HTTP 方法",
    "type": "string",
    "default": "get",
    "enum": ["get", "post"]
  }
}
